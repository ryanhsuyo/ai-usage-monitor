# Dependencies
node_modules
.pnpm-store

# Build output
dist
dist-ssr
*.local

# Rust / Tauri
src-tauri/target
src-tauri/gen

# Editor / OS
.DS_Store
.idea
.vscode/*
!.vscode/extensions.json
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Test / coverage
coverage
*.log

# Local machine-specific harness config (never commit)
.claude/settings.local.json

# Local data / secrets (never commit)
*.db
*.db-journal
*.sqlite
secrets.local.json
